server:
  port: 8888
spring:
  application:
    name: boss-config
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/properties/
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka
