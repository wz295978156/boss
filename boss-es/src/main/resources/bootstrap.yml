server:
  port: 8085
spring:
  application:
    name: boss-es
  cloud:
    config:
      discovery:
        enabled: true
        service-id: boss-config
      name: neo-config
      profile: dev
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka
